# macOS
.DS_Store

# Claude Code local state
.claude/

# Python bytecode (from running the shared validator/export scripts)
__pycache__/
*.pyc

# Packaged skills (distributed via GitHub releases)
*.skill

# research-summary output files (generated in user workspace)
*_Evidence_Summary_*.md
*_Evidence_Summary_*.docx

# Zotero exports written by both consumer skills (generated in user workspace)
*_References.bib
*_PMIDs.txt

# protocol-reviewer output files (generated in user workspace)
*_Review_*.md
*_Review_*.docx

# Hidden reference ledger — canonical internal handoff artifact
.literature_search_ledger.yaml

# Legacy ledger filenames from pre-1.0 layouts — keep ignored so stray
# files from old searches never get committed
*_References.yaml
_working_ledger.yaml

# protocol-reviewer evaluation register (written to the user's workspace;
# contains review-specific audit data and must never be committed to a public repo).
# reviews/ is the pre-2.0.1 location, kept ignored for stray copies.
clinical-evidence-register.csv
reviews/

# Development scratch files
IMPROVEMENT_PLAN.md
